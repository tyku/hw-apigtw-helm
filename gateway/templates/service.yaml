---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw-apigtw-helm.fullname" . }}
spec:
  type: {{ .Values.gateway.service.type }}
  selector:
    app: auth-service
  ports:
    - port: 80
      name: http
      targetPort: http
